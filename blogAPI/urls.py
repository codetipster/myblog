from django.urls import path
# from .views import PostList, PostDetail

app_name = 'blogAPI'

urlpatterns = [
    # path('<int:pk>/', PostDetail.as_view(), name="ViewPost"),  #shows individual post in database
    # path('', PostList.as_view(), name="AllPosts"),  #shows the list of all posts in database
]